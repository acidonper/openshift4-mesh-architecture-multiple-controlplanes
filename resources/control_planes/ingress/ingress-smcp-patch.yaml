spec:
  gateways:
    ingress:
      volumes:
      - volume:
          secret:
            secretName: istio-mtls-cp-secret
        volumeMount:
          mountPath: /etc/istio/mtls-certs
          name: istio-mtls-cp-secret